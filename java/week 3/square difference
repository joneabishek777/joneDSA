class Solution {
    static long differenceOfSums(long N) {
        long sumSquares = N * (N + 1) * (2 * N + 1) / 6;  
        long sum = N * (N + 1) / 2;
        long squareOfSum = sum * sum;

        return Math.abs(sumSquares - squareOfSum);
    }
}
