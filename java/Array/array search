class Solution {
    public int search(int arr[], int x) {
        // code here
        int flag = -1;
        for(int i=0;i<arr.length;i++){
            if(arr[i]==x){
                flag = i;
                return flag;
            }

        }
        
        return flag;      
    }
}
