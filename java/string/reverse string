class Solution {
    public static String reverseString(String s) {
        // code here
        String copys = "";
        for(int i=s.length()-1;i>=0;i--){
            char c = s.charAt(i);
            copys = copys+c;
        }
        s= copys;
        return s;
    }
}
